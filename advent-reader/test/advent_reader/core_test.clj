(ns advent-reader.core-test
  (:require [clojure.test :refer :all]
            [advent-reader.core :refer :all]))

(deftest parse-int-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest parse-separate-empty-lines-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest parse-split-by-character-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
